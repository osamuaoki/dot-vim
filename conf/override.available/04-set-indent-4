" Indentation to N=4, expand tab to spaces
 
" tab code spacing
set tabstop=4

" shift with > < moves N
set shiftwidth=4

" soft tab is N 
set softtabstop=4

" Don't use tab code, expand it
set expandtab
