" vim: set sw=2 sts=2 et ft=vim :
" Indentation to N=4, expand tab to spaces

" tab code spacing
set tabstop=8

" shift with > < moves N
set shiftwidth=8

" soft tab is N
set softtabstop=8

" Don't use tab code, expand it
set expandtab
